name: Nightly Stat Leaders

on:
  schedule:
    # Runs daily at 1:00 AM New York (6:00 UTC)
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Generate + Upload Stat Leaders
        env:
          SUPABASE_URL: ${{ secrets.https://rojsabkwywygludonpdf.supabase.co }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InJvanNhYmt3eXd5Z2x1ZG9ucGRmIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc2NjUxMTA2NSwiZXhwIjoyMDgyMDg3MDY1fQ.7HIE5trccv7GDqxVZGDw9kBFLi1PVhuCQTNYUkpoL_k }}
          SUPABASE_BUCKET: ${{ secrets.nfl-posters }}
        run: |
          python app/generator/stat_leaders/run_nightly.py
